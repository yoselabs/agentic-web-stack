Feature: {{pascalCase name}}

  # Scaffolded by `turbo gen feature`. Replace the placeholder scenario with
  # real behavior specs. See e2e/CLAUDE.md for declarative-style rules,
  # scenario budgets (3-7 steps), and the BDD-first workflow.
  #
  # Recommended orthogonal axes to cover (see e2e/CLAUDE.md):
  #   - Happy path
  #   - Empty state
  #   - Authorization / privacy
  #   - State transitions (if applicable)

  Scenario: {{pascalCase name}} scaffold placeholder
    Given I am signed in as "{{name}}-scaffold@example.com"
    When I visit the {{name}} placeholder
    Then I should see "{{pascalCase name}} scaffold"
