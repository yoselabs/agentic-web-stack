/**
 * Step definitions for the {{name}} feature. See e2e/CLAUDE.md §
 * "Writing Step Definitions" for locator priority, reuse rules, and the
 * precondition-auth-via-HTTP pattern.
 *
 * The scaffolded step below is unique to this feature (no phrasing
 * collision with shared steps in e2e/steps/auth/auth.ts). Replace with
 * real step definitions as the scenarios in `{{name}}.feature` grow.
 */
import { createBdd } from "playwright-bdd";

const { When: when } = createBdd();

when("I visit the {{name}} placeholder", async ({ page }) => {
  // Scaffold: no real page to visit yet. Once the route exists, replace
  // with a typed `page.goto("/")` or scoped navigation.
  void page;
});
