/**
 * {{pascalCase name}}Panel — composed UI block for the {{name}} feature.
 *
 * Widgets combine features + UI components (see apps/web/CLAUDE.md §
 * "FSD"). Keep this file renderable-without-context so Storybook can
 * mount it — pass live data via props, never read route context here.
 */

export type {{pascalCase name}}PanelProps = {
  title?: string;
};

export function {{pascalCase name}}Panel({
  title = "{{pascalCase name}}",
}: {{pascalCase name}}PanelProps) {
  return (
    <section
      aria-label="{{name}} panel"
      className="rounded-md border p-4"
      data-testid="{{name}}-panel"
    >
      <h2 className="font-semibold text-lg">{title}</h2>
      <p className="text-muted-foreground text-sm">
        Scaffolded {{name}} panel. Replace with real UI.
      </p>
    </section>
  );
}
