{
  "name": "@project/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sh -c '[ -f ../../.env ] && exec bun --watch --env-file=../../.env src/index.ts || exec bun --watch src/index.ts'",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@bull-board/api": "catalog:",
    "@bull-board/hono": "catalog:",
    "@hono/node-server": "^1.19.14",
    "@hono/trpc-server": "^0.3.4",
    "@project/api": "workspace:*",
    "@project/auth": "workspace:*",
    "@project/db": "workspace:*",
    "@project/env": "workspace:*",
    "@project/jobs": "workspace:*",
    "@project/rate-limit": "workspace:*",
    "@trpc/server": "^11.16.0",
    "hono": "^4.12.14",
    "pino": "^10.3.1",
    "ws": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/ws": "catalog:",
    "pino-pretty": "^13.1.3"
  }
}
