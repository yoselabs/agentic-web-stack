{
  "extends": "../../tsconfig.base.json",
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["dist"],
  "compilerOptions": {
    "target": "ES2022",
    "jsx": "react-jsx",
    // `baseUrl` is deprecated in TS 6 and removed in TS 7. Dropping it —
    // `paths` values are relative to the tsconfig's location, which gives
    // the same resolution for our `#/*` alias.
    "paths": {
      "#/*": ["./src/*"]
    },
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": false,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "composite": true,
    "emitDeclarationOnly": true,
    "declaration": true,
    "declarationMap": true,
    "outDir": "dist"
  },
  "references": [{ "path": "../../packages/ui" }]
}
