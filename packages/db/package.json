{
  "name": "@project/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "generate": "bun run scripts/generate.ts",
    "push": "prisma db push",
    "studio": "prisma studio",
    "migrate": "prisma migrate dev",
    "postinstall": "bun run scripts/generate.ts"
  },
  "dependencies": {
    "@prisma/client": "catalog:",
    "@project/env": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "prisma": "catalog:"
  }
}
